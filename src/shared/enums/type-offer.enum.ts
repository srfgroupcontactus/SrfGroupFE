export enum TypeOfferEnum {
  Sell = "SellOffer",
  Rent = "RentOffer",
  Find = "FindOffer",
}

export enum PeriodeRent {
  PerYear = "PerYear",
  PerMonth = "PerMonth",
  PerDay = "PerDay",
}

export enum TypeDisplaySearchOffers {
  List = "LIST",
  Grid = "GRID",
}
